- [ ] Restore Frontend/exams.html back to valid HTML (remove corrupted BOM/garbage at top)
- [ ] Add exam filtering UI handlers (search + level + clear) wired to exam cards
- [ ] Ensure every exam card has correct `data-exam-level` attribute
- [ ] Verify page still opens and `startExam()` navigation works
- [ ] Run quick local check (open in browser / run linter if available)

